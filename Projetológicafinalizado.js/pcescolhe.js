function getComputerChoice (){
    Math.random() * (2 - 0) + 0;
      
      min = Math.ceil(0);
      max = Math.floor(2);
      
      return Math.floor(Math.random() * (2 - 0)) + 0;
      
}

const valor_recebido = (0, 2)
 switch (valor_recebido){
   case 0:
   console.log("pedra")
     break;
   case 1:
    console.log("papel")
     break;
   case 2:
    console.log("tesoura")
     break;
     
 }
